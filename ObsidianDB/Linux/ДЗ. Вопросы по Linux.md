
---
## 1. Общие основы Linux

1. Что такое Linux? Чем отличаются Linux, GNU/Linux и дистрибутив?
2. Что такое ядро ОС? За что отвечает ядро Linux?
3. Какие основные семейства дистрибутивов Linux ты знаешь? Приведи примеры.
4. Чем отличаются Debian/Ubuntu, RHEL/Fedora и Arch Linux с точки зрения пакетного менеджмента и подхода к обновлениям?
5. Что такое shell? Какие оболочки ты знаешь?
6. Что такое терминал, консоль и TTY? В чём различие?
7. Что такое root и почему не следует постоянно работать от root?
8. Что такое `sudo`? Чем оно отличается от `su`?
9. Как узнать текущего пользователя, его UID, группы и текущую директорию?
10. Как получить справку по команде через `man`, `--help`, `help`, `info`?
11. Что означают коды возврата команд? Как получить код последней команды?
12. Что такое переменная окружения? Как посмотреть, создать и экспортировать её?
13. Что такое `PATH` и как shell ищет исполняемую программу?
14. Что такое алиасы? Как создать временный и постоянный alias?
15. Что такое история команд? Как искать и повторно выполнять команды из истории?

---

## 2. Файловая система

1. Что такое иерархия файловой системы (FHS)?
2. За что обычно отвечают каталоги `/`, `/home`, `/root`, `/etc`, `/var`, `/usr`, `/opt`, `/tmp`, `/dev`, `/proc`, `/sys`, `/run`?
3. В чём разница между абсолютным и относительным путём?
4. Что означают `.` и `..` в путях?
5. Чем отличаются команды `pwd`, `cd`, `ls`? Назови полезные опции `ls`.
6. Как создать файл и директорию? Как создать вложенные директории одной командой?
7. Как копировать, перемещать, переименовывать и удалять файлы и директории?
8. Чем опасна команда `rm -rf`? Какие меры предосторожности нужны?
9. Что такое glob-шаблоны? Объясни `*`, `?`, `[]`, `{}`.
10. Что такое inode? Какие данные в нём хранятся и почему имя файла не является частью inode?
11. Чем отличаются hard link и symbolic link? Когда hard link нельзя создать?
12. Как создать и просмотреть символическую ссылку?
13. Что такое файловая система? Какие файловые системы Linux ты знаешь?
14. Что такое монтирование? Как посмотреть смонтированные файловые системы?
15. Чем отличаются `df` и `du`? Как найти каталоги, занимающие много места?
16. Что такое `/etc/fstab`? Какие риски есть при его неправильном редактировании?
17. Что такое UUID раздела и зачем его используют в `/etc/fstab`?
18. Что такое виртуальные файловые системы `/proc` и `/sys`? Приведи примеры полезных файлов в них.
19. Что означает «в Linux всё является файлом»? Какие есть исключения или уточнения?
20. Какие типы файлов показывает первый символ в `ls -l`?

---

## 3. Права и владельцы

1. Что означают права `r`, `w`, `x` для обычного файла и для директории?
2. Как читать строку прав вида `-rwxr-x---`?
3. Кто такие владелец файла, группа файла и остальные пользователи?
4. Как изменить права через символьную и восьмеричную нотацию `chmod`?
5. Что означают режимы `755`, `644`, `700`, `600`? Где они обычно применяются?
6. Как изменить владельца и группу файла командами `chown` и `chgrp`?
7. Что такое `umask`? Как она влияет на права новых файлов и директорий?
8. Что такое SUID, SGID и sticky bit? Приведи пример применения sticky bit.
9. Почему SUID на исполняемых файлах потенциально опасен?
10. Что такое ACL? Когда стандартных прав owner/group/other недостаточно?
11. Как посмотреть и изменить ACL через `getfacl` и `setfacl`?
12. Почему небезопасно делать `chmod 777`? Какие альтернативы лучше?

---

## 4. Работа с текстом и потоками

1. Что такое стандартные потоки `stdin`, `stdout`, `stderr`? Какие у них файловые дескрипторы?
2. Что такое перенаправление потоков? Объясни `>`, `>>`, `<`, `2>`, `2>&1`.
3. Что такое pipe (`|`)? Приведи пример конвейера команд.
4. Как работают `cat`, `less`, `head`, `tail`? Когда использовать `tail -f`?
5. Чем отличаются `echo` и `printf`?
6. Как искать текст с помощью `grep`? Что делают опции `-r`, `-n`, `-i`, `-v`, `-E`?
7. Что такое регулярные выражения? Чем BRE отличается от ERE?
8. Для чего используются `sed` и `awk`? Приведи по одному примеру.
9. Что делают `sort`, `uniq`, `wc`, `cut`, `tr`, `tee`?
10. Как найти файлы с помощью `find`? Чем `find` отличается от `locate`?
11. Как безопасно обрабатывать имена файлов с пробелами и переводами строк в shell-скриптах?
12. Что такое `xargs`? Чем полезна опция `-0`?
13. Как сравнить два текстовых файла? Что показывают `diff` и `cmp`?
14. Как работать с архивами `tar`, `gzip`, `bzip2`, `xz`, `zip`?
15. Как создать и распаковать архив `.tar.gz`?

---

## 5. Процессы и задания

1. Что такое процесс? Чем программа на диске отличается от процесса?
2. Что такое PID, PPID, foreground- и background-процесс?
3. Как посмотреть процессы с помощью `ps`, `top`, `htop`, `pgrep`?
4. Чем отличаются `ps aux` и `ps -ef`?
5. Как запустить команду в фоне? Что делают `&`, `jobs`, `bg`, `fg`?
6. Что произойдёт с фоновым процессом при закрытии терминала? Для чего нужны `nohup`, `disown`, `screen`, `tmux`?
7. Что такое сигнал процесса? Назови сигналы `SIGTERM`, `SIGKILL`, `SIGHUP`, `SIGINT`, `SIGSTOP`.
8. Чем отличаются `kill`, `pkill` и `killall`?
9. Почему `SIGKILL` стоит использовать только в крайнем случае?
10. Что такое zombie-процесс и почему он появляется?
11. Что такое orphan-процесс? Какую роль играет PID 1?
12. Что такое приоритет процесса? Как используются `nice` и `renice`?
13. Как ограничить потребление ресурсов процессом? Что такое `ulimit`?
14. Что такое файловый дескриптор процесса? Как посмотреть открытые файлы через `lsof`?
15. Как узнать, какой процесс использует порт или файл?

---

## 6. Загрузка и службы

1. Какие основные этапы загрузки Linux: firmware, bootloader, kernel, initramfs, init/systemd?
2. Что такое GRUB и для чего он нужен?
3. Что такое initramfs?
4. Что такое systemd? Какую роль играет PID 1?
5. Что такое unit в systemd? Какие типы unit-файлов ты знаешь?
6. Как проверить состояние сервиса через `systemctl`?
7. Как запустить, остановить, перезапустить и перезагрузить конфигурацию сервиса?
8. Чем отличаются `enable`, `start`, `disable`, `stop`, `mask` у `systemctl`?
9. Как посмотреть логи сервиса с помощью `journalctl`?
10. Чем отличаются логи текущей загрузки и предыдущей загрузки в `journalctl`?
11. Что такое target в systemd? С чем его можно сопоставить из SysV runlevel?
12. Где обычно хранятся unit-файлы systemd и локальные override-настройки?
13. Что такое systemd timer? Чем он отличается от cron?
14. Как создать простой собственный systemd-сервис?
15. Как диагностировать сервис, который не запускается?

---

## 7. Сеть

1. Что такое IP-адрес, маска/префикс сети, шлюз по умолчанию и DNS?
2. Чем отличаются IPv4 и IPv6?
3. Что такое интерфейс loopback и адрес `127.0.0.1`?
4. Как посмотреть сетевые интерфейсы, адреса, маршруты и ARP/neighbor-таблицу с помощью `ip`?
5. Почему `ifconfig` считается устаревшим и чем его заменяют?
6. Как проверить доступность хоста через `ping`? Почему успешный `ping` не гарантирует доступность сервиса?
7. Как проверить DNS-разрешение через `dig`, `host`, `nslookup`?
8. Что хранится в `/etc/hosts`, `/etc/resolv.conf`, `/etc/nsswitch.conf`?
9. Что такое TCP и UDP? В чём ключевая разница?
10. Что такое порт и сокет? Как посмотреть слушающие порты через `ss`?
11. Как проверить HTTP-сервис с помощью `curl`? Что делают `-I`, `-v`, `-f`?
12. Что такое SSH? Как подключаться по ключу и почему это безопаснее пароля?
13. Как создать SSH-ключ? Чем отличаются открытый и приватный ключи?
14. Для чего нужны `scp`, `sftp` и `rsync`? Когда выбрать каждый инструмент?
15. Что такое firewall? Какую роль играют `nftables`, `iptables`, `ufw`, `firewalld`?
16. Что такое NAT и для чего он используется?
17. Как диагностировать сетевую проблему: DNS, маршрут, порт, firewall, приложение?

---

## 8. Управление пакетами

1. Что такое пакет, репозиторий и зависимость?
2. Чем отличаются пакетные менеджеры `apt`, `dnf`, `pacman`, `zypper`?
3. Как в Debian/Ubuntu обновить индекс пакетов, обновить систему, установить, удалить и найти пакет?
4. Чем отличаются `apt remove`, `apt purge`, `apt autoremove`?
5. В чём разница между `apt` и `apt-get`?
6. Как узнать, какому пакету принадлежит файл и какие файлы установил пакет?
7. Как посмотреть информацию, версию и зависимости пакета?
8. Что такое `.deb` и `.rpm`? Почему не стоит устанавливать пакеты из непроверенных источников?
9. Что такое PPA и какие риски есть у сторонних репозиториев?
10. Что такое Snap, Flatpak и AppImage? Чем они отличаются от системных пакетов?
11. Почему нельзя бездумно смешивать репозитории разных версий дистрибутива?
12. Как безопасно обновлять сервер перед важным изменением?

---

## 9. Пользователи и безопасность

1. Где хранятся сведения о пользователях и группах: `/etc/passwd`, `/etc/shadow`, `/etc/group`?
2. Как создать, изменить и удалить пользователя и группу?
3. Как добавить пользователя в дополнительную группу?
4. Почему парольные хеши находятся в `/etc/shadow`, а не в `/etc/passwd`?
5. Как устроен sudo-доступ? Почему `visudo` предпочтительнее прямого редактирования `/etc/sudoers`?
6. Что такое PAM и какую роль он играет при аутентификации?
7. Как защитить SSH-сервер: ключи, запрет root login, ограничение пользователей, обновления?
8. Что такое принцип наименьших привилегий?
9. Что такое capability в Linux и чем она отличается от полного root-доступа?
10. Что такое SELinux и AppArmor? Как они дополняют обычные Unix-права?
11. Что такое аудит безопасности и какие логи важны при расследовании инцидента?
12. Почему секреты нельзя передавать в аргументах командной строки и хранить в Git?

---

## 10. Bash и shell-скрипты

1. Как устроен shell-скрипт? Зачем нужен shebang `#!/usr/bin/env bash`?
2. Как сделать скрипт исполняемым и запустить его?
3. Чем отличаются `sh script.sh`, `bash script.sh` и `./script.sh`?
4. Как объявлять и использовать переменные в Bash? Почему важны кавычки?
5. Чем отличаются одинарные, двойные кавычки и отсутствие кавычек?
6. Что такое command substitution: `$(command)`?
7. Что такое exit status? Как использовать `$?`, `exit`, `&&`, `||`?
8. Как работают условия `if`, `case`, `test`, `[ ]` и `[[ ]]`?
9. Как работают циклы `for`, `while`, `until`? Приведи примеры.
10. Что такое функции Bash? Как передавать им аргументы и возвращать статус?
11. Что означают `$0`, `$1`, `$#`, `$@`, `$*`, `$$`?
12. Что такое массивы Bash и какие у них ограничения?
13. Что делает `set -euo pipefail`? Какие у него подводные камни?
14. Что такое trap? Как с его помощью очищать временные файлы при завершении скрипта?
15. Как передавать параметры в скрипт? Как использовать `getopts`?
16. Чем shell-скрипт отличается от Python-скрипта? Когда Bash — плохой выбор?
17. Как проверять shell-скрипты с помощью ShellCheck?

---

## 11. Планирование задач

1. Что такое cron и для каких задач он подходит?
2. Как устроен формат записи crontab из пяти полей?
3. Чем отличаются пользовательский crontab и системный `/etc/crontab`?
4. Почему задачи cron часто «не работают» так же, как в интерактивном терминале?
5. Как перенаправить stdout и stderr cron-задачи в лог?
6. Что такое `at` и когда он полезнее cron?
7. Что такое systemd timer? Назови преимущества перед cron.
8. Как диагностировать невыполнение периодической задачи?

---

## 12. Диски, память и производительность

1. Как посмотреть информацию о дисках и разделах через `lsblk`, `blkid`, `fdisk`, `parted`?
2. Что такое раздел, GPT и MBR? В чём различие GPT и MBR?
3. Что такое LVM? Назови сущности PV, VG, LV и преимущества LVM.
4. Что такое swap? Когда он используется и почему его наличие не заменяет RAM?
5. Как посмотреть использование оперативной памяти через `free`, `vmstat`, `/proc/meminfo`?
6. Что такое page cache? Почему «занятая» память не всегда означает проблему?
7. Как посмотреть загрузку CPU, памяти, диска и I/O wait?
8. Что такое load average? Почему его нельзя интерпретировать без числа CPU и состояния задач?
9. Чем отличаются `df` и `du`, если они показывают разный объём занятого места?
10. Почему удалённый, но открытый процессом файл продолжает занимать место?
11. Как обнаружить такой файл с помощью `lsof`?
12. Что такое OOM killer? Что может произойти при нехватке памяти?
13. Что такое cgroups и как контейнеры используют их для ограничений ресурсов?
14. Как безопасно увеличить размер файловой системы или логического тома?

---

## 13. Docker и Linux

1. Чем контейнер отличается от виртуальной машины?
2. Какие механизмы ядра Linux лежат в основе контейнеров: namespaces, cgroups, capabilities?
3. Почему контейнер использует ядро хостовой ОС?
4. Что такое образ, контейнер, слой образа и Dockerfile?
5. Чем отличаются `CMD` и `ENTRYPOINT` в Dockerfile?
6. Что такое volume и bind mount? Когда применять каждый вариант?
7. Что означает публикация порта `-p 8080:80`?
8. Почему процесс с PID 1 в контейнере требует особого внимания к сигналам и дочерним процессам?
9. Почему не следует запускать приложение в контейнере от root без необходимости?
10. Как уменьшить размер образа и количество уязвимостей?
11. Чем отличаются Docker Compose и Kubernetes на базовом уровне?
12. Как посмотреть логи, процессы и использование ресурсов контейнера?

---

## 14. Диагностика и отладка

1. Как получить базовую информацию о системе: ядро, дистрибутив, CPU, память, диски?
2. Как посмотреть сообщения ядра через `dmesg` и journalctl?
3. Как искать ошибки в логах с помощью `journalctl` и `grep`?
4. В каких каталогах традиционно лежат логи? Почему в systemd-системах часть логов может быть только в journal?
5. Что такое `strace`? Какие задачи он помогает решать?
6. Что такое `ltrace` и чем он отличается от `strace`?
7. Что такое `ldd`? Почему нельзя бездумно запускать его на недоверенных исполняемых файлах?
8. Как узнать, какие библиотеки загрузил процесс?
9. Как понять, почему порт занят?
10. Как диагностировать ошибку «Permission denied»?
11. Как диагностировать ошибку «No space left on device», если `df` показывает свободное место?
12. Как собрать минимально достаточные данные для обращения к коллеге или в issue?

---

## 15. Git и разработка в Linux

1. Как настроить Git: имя, email, редактор, SSH-ключ?
2. Чем отличаются рабочая директория, staging area и репозиторий Git?
3. Как проверить состояние, добавить изменения, создать коммит и посмотреть историю?
4. Чем отличаются `git fetch`, `git pull` и `git push`?
5. Как использовать `.gitignore`? Почему он не перестаёт отслеживать уже добавленный файл?
6. Что такое ветка, merge и rebase? В чём риск rebase опубликованной ветки?
7. Как разрешать merge-конфликты?
8. Как отменить изменения: `restore`, `reset`, `revert`? Когда выбрать каждую команду?
9. Почему нельзя коммитить секреты? Что делать, если секрет уже попал в историю?
10. Как настроить pre-commit hooks для форматирования и проверок?

---

## 16. Практические задания

1. Создай пользователя без root-доступа, добавь его в нужную группу и настрой вход по SSH-ключу.
2. Напиши Bash-скрипт резервного копирования каталога в `.tar.gz`: проверка аргументов, логирование, удаление архивов старше N дней.
3. Создай systemd unit для собственного приложения и добейся его автозапуска после перезагрузки.
4. Создай systemd timer или cron-задачу для ежедневного бэкапа и проверь логи выполнения.
5. Найди процесс, который слушает порт, заверши его корректно и убедись, что порт освободился.
6. Найди 10 самых больших файлов в заданном каталоге, корректно обрабатывая пробелы в именах.
7. Настрой права на каталог проекта так, чтобы группа могла читать и изменять файлы, а остальные не имели доступа.
8. Подними локальный HTTP-сервис в Docker, проверь его через `curl`, просмотри логи и ограничь доступные ему ресурсы.
9. Смоделируй переполнение диска: найди причину, освободи место и проверь, не остались ли удалённые открытые файлы.
10. Проведи первичную диагностику недоступного сервиса: DNS, маршрут, порт, firewall, systemd-статус, логи.

---

## Рекомендуемый минимум команд

`pwd`, `cd`, `ls`, `cp`, `mv`, `rm`, `mkdir`, `touch`, `ln`, `find`, `grep`, `sed`, `awk`, `xargs`, `tar`, `chmod`, `chown`, `ps`, `top`, `kill`, `systemctl`, `journalctl`, `ip`, `ss`, `ping`, `dig`, `curl`, `ssh`, `rsync`, `apt`, `df`, `du`, `lsblk`, `free`, `lsof`, `strace`, `git`.