## Что такое идиома RAII? Напиши код. Приведи примеры RAII в STL.

## Что такое RVO? Напиши код.
RVO - (Return Value Optimization) - оптимизация компилятора, позволяющая в некоторых случаях не создавать локальный объект, который будет использован как возвращаемое значение

RVO неразрывно связано с понятием `prvalue` (`pure value`)

Что происходит с кодом без `RVO` (флаг `no-elide-constructors` в `g++` компиляторе):
```C++
class A {
public:
  A() { std::cout << "A()" << std::endl; }
  A(const A &) { std::cout << "A(const A&)" << std::endl; }

  ~A() { std::cout << "~A()" << std::endl; }
};

A getA() {
  auto x = A(); // (1)
  return x;
}

int main() {
  auto a = getA(); // (2)
  return 0;
}
```
Создание объекта `a`  сотоит из нескольких этапов:
1) Создание локального объекта `x` внтри функции `getA`
2) Копирование содержимого объекта `x` в переменную `a`
3) Вызов деструктора для объекта `x`

До `C++17` вызов 




